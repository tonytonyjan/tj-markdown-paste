<!DOCTYPE html>
<html lang="en">
  <head itemscope itemtype="http://schema.org/WebApplication">
    <meta charset="utf-8">
    
    <%= render partial: 'layouts/title_and_metas' %>

    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>

    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <!-- Fav and touch icons -->
    <link rel="shortcut icon" href="../assets/ico/favicon.ico">
    <link rel="apple-touch-icon-precomposed" sizes="144x144" href="../assets/ico/apple-touch-icon-144-precomposed.png">
    <link rel="apple-touch-icon-precomposed" sizes="114x114" href="../assets/ico/apple-touch-icon-114-precomposed.png">
    <link rel="apple-touch-icon-precomposed" sizes="72x72" href="../assets/ico/apple-touch-icon-72-precomposed.png">
    <link rel="apple-touch-icon-precomposed" href="../assets/ico/apple-touch-icon-57-precomposed.png">
    <%= yield :head %>
  </head>

  <body class="<%= controller_name %> <%= action_name %>" data-controller="<%= controller_name %>" data-action="<%= action_name %>">
    
    <% if content_for? :container %>
      <%= yield :container %>
    <% else %>
      <div class="container-narrow">
        <div class="masthead">
          <%= render partial: 'layouts/navbar' %>
          <h3 class="muted"><%= t("site_name") %></h3>
        </div>

        <%= notice_message %>

        <hr>

        <%= yield %>

        <hr>

        <div class="footer">
          <p>&copy; <%= link_to "tonytonyjan", "http://tonytonyjan.github.com", target: "_blank" %> 2012</p>
        </div>
      </div> <!-- /container -->
    <% end %>
    <%= yield :body %>
    <%= render partial: 'layouts/google_plus_api' %>
    <%= render partial: 'layouts/facebook_api' %>
    <%= render partial: 'layouts/google_analytics' %>
  </body>
</html>

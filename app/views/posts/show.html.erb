<%= render 'mathjax' %>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.markup_content %>
</p>


<%= link_to t('tj.form.edit'), edit_post_path(@post) %> |
<%= link_to t('tj.form.back'), posts_path %>